class Solution {
    public int findNumbers(int[] nums) {
        int count = 0;
        for (int num : nums) {
            if ((Integer.toString(num).length() & 1) == 0) {
                count++;
            }
        }
        return count;
    }
}
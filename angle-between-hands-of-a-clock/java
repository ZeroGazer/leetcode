class Solution {
    public double angleClock(int hour, int minutes) {
        double angle = Math.abs(minutes * 6 - (hour % 12 * 30 + minutes * 0.5));
        return Math.min(angle, 360 - angle);
    }
}
class Solution {
    public int minSwaps(String s) {
        int opened = 0;
        for (char c : s.toCharArray()) {
            if (c == '[') {
                opened++;
            } else {
                if (opened > 0) {
                    opened--;
                }
            }
        }
        return (opened + 1) / 2;
    }
}
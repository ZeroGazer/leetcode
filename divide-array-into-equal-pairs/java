class Solution {
    public boolean divideArray(int[] nums) {
        int[] freqs = new int[501];
        int odd = 0;
        for (int num : nums) {
            if ((++freqs[num] & 1) == 0) {
                odd--;
            } else {
                odd++;
            }
        }
        return odd == 0;
    }
}
class Solution {
    public int largestAltitude(int[] gain) {
        int curr = 0, max = 0;
        for (int diff : gain) {
            curr += diff;
            max = Math.max(max, curr);
        }
        return max;
    }
}
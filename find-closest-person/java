class Solution {
    public int findClosest(int x, int y, int z) {
        int tx = Math.abs(x - z), ty = Math.abs(y - z);
        if (tx < ty) {
            return 1;
        } else if (tx > ty) {
            return 2;
        } else {
            return 0;
        }
    }
}